feat: ship 31 session updates (vendor apply map, passport routing, logo, billing inputs; dual-screen toolbar + room-scoped canvas bounds; room overlap boolean union; middle-button pan GPU preview; +27 more)