/** Homepage hero — platform-level value prop (HubGuard stays on /check and vendor surfaces). */
export const HOME_HERO = {
  eyebrow: 'Built in Canada · strong in Alberta today',
  headline: 'One hub for local makers markets',
  subhead:
    'Patrons discover who is vending. Artisans apply with one passport. Coordinators run applications, booth layouts, and market day — wherever your community runs markets.',
} as const

export const COORDINATOR_SIGNUP_HREF =
  '/signup?role=coordinator&next=/coordinator/events/new'

/** Ad listing tier — list on Discover without native booth ops. */
export const COORDINATOR_ADVERTISE_SIGNUP_HREF =
  '/signup?role=coordinator&next=/coordinator/events/advertise'

/** Intent vs impressions marketing comparison page. */
export { COMPARE_INTENT_HREF } from '@/lib/marketing/compare-intent'
