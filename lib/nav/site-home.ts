/** Marketing landing — three path cards (patron / vendor / coordinator). */
export const SITE_HOME_PATH = '/' as const

/** Default post-auth and app-launch landing when no explicit deep link is provided. */
export const DEFAULT_START_PATH = SITE_HOME_PATH
